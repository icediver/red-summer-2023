import { IShipmentData } from '@/data/cities';
import { FC } from 'react';


const SearchList: FC<IShipmentData[]> = (props) => {

    return (
        <div>SearchList</div>
    );
};
export default SearchList;
